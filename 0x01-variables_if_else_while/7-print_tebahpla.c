#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: 0 if success
 */
int main(void)
{
	char d;

	for (d = 'z'; d >= 'a'; d--)
	{

		putchar(d);

	}

	putchar('\n');

	return (0);
}
