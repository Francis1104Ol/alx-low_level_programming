#include "main.h"
/**
 * _abs - functions that computes the absolute value of an intege.
 * @r: parameter of numbers
 *
 * Return: 0
 */
int _abs(int r)
{

	if (r > 0)
	{
		return (r);
	}
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
}
